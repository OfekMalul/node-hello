pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'ls -l'
                sh 'echo "hello world" '
                sh 'node --version'
            }
        }
    }
}