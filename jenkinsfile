pipeline {
  agent {
    label 'agent1'
  }
  stages {
    stage('Test') {
      steps {
        sh 'ls -l'
        sh 'whoami'
        sh 'groups'
        sh 'docker images'
      }
    }
  }
    post{
	always{
	   sh 'docker ps'
	   sh 'docker stop $(docker ps -a -q)'
    }
    } 
}